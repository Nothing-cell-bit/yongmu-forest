# 贡献指南

修改代码或文档前，请阅读README中的目录、开发方式和许可说明。

1. 将一次修改限定在一个可说明的问题内，解释修改前后行为。
2. 运行`python tools/run_public_tests.py`，按改动补充必要的针对性验证。
3. 如果涉及SDK或资源，列出需要的输入文件与许可，不提交没有分发依据的素材。
4. 修改图片时更新`ASSET_MANIFEST.json`中的来源、改动说明与校验值，保留适用的CC许可和署名。
5. PR中写清测试环境、结果与尚未验证的内容。没有真实客户端证据时，不把状态标为运行时通过。

不要提交账户凭据、Cookie、Token、私人日志、存档、缓存或未经核对的第三方文件。代码与图片分别遵循仓库中的相应许可证。
