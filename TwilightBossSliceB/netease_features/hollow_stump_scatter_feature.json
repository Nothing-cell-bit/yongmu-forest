{
  "format_version": "1.20.30",
  "minecraft:scatter_feature": {
    "description": {
      "identifier": "tf_slice:hollow_stump_scatter_feature"
    },
    "places_feature": "tf_slice:hollow_stump_structure_selector_feature",
    "iterations": "(math.max(math.max(math.max(query.get_height_at(variable.originx + 0, variable.originz + 0), query.get_height_at(variable.originx + 14, variable.originz + 0)), query.get_height_at(variable.originx + 0, variable.originz + 14)), query.get_height_at(variable.originx + 14, variable.originz + 14)) - math.min(math.min(math.min(query.get_height_at(variable.originx + 0, variable.originz + 0), query.get_height_at(variable.originx + 14, variable.originz + 0)), query.get_height_at(variable.originx + 0, variable.originz + 14)), query.get_height_at(variable.originx + 14, variable.originz + 14)) <= 2) ? 1 : 0",
    "scatter_chance": {
      "numerator": 1,
      "denominator": 80
    },
    "x": 0,
    "y": 0,
    "z": 0
  }
}
