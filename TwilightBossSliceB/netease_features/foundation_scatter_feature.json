{
  "format_version": "1.20.30",
  "minecraft:scatter_feature": {
    "description": {
      "identifier": "tf_slice:foundation_scatter_feature"
    },
    "places_feature": "tf_slice:foundation_structure_selector_feature",
    "iterations": "(math.max(math.max(math.max(query.get_height_at(variable.originx + 0, variable.originz + 0), query.get_height_at(variable.originx + 9, variable.originz + 0)), query.get_height_at(variable.originx + 0, variable.originz + 9)), query.get_height_at(variable.originx + 9, variable.originz + 9)) - math.min(math.min(math.min(query.get_height_at(variable.originx + 0, variable.originz + 0), query.get_height_at(variable.originx + 9, variable.originz + 0)), query.get_height_at(variable.originx + 0, variable.originz + 9)), query.get_height_at(variable.originx + 9, variable.originz + 9)) <= 2) ? 1 : 0",
    "scatter_chance": {
      "numerator": 1,
      "denominator": 90
    },
    "x": 0,
    "y": 0,
    "z": 0
  }
}
